package com.chetaru.tribe365_new.UI.UserInfo

class UserUnitTest {
     //Global setUp
    private val first = "Alice"
    private val last = "simmons"
   /* private val user = JavaUser(first, last)
    private val userName = JavaUser(first,last)
    private val userDifferent = JavaUser("Alex","Smith")

    @Test
    fun readProperties(){
        assertEquals(first,user.first)
        assertEquals(last,user.last)
    }

    @Test
    fun writeProperties(){
        user.first = "Alex"
        user.last= "smith"
        assertEquals(first,user.first)
        assertEquals(last, user.last)
    }
    @Test
    fun checkHashCode(){
        assertEquals(user.hashCode(),userDifferent.hashCode())
    }

    @Test
    fun checkEquals(){
        assert(user==userName)
    }
    @Test
    fun checkNotEquals(){
        assert(user != userDifferent)
    }*/
}